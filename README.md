# Bilal Nadeem — Portfolio (React)

Built with React + Vite + Tailwind CSS v4 + Framer Motion.

## Run locally
```
npm install
npm run dev
```

## Build
```
npm run build
```

## Deploy
Push to GitHub and import into Vercel — it auto-detects Vite and deploys with zero config.
